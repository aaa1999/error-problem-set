# 错题本（单机跨平台）设计文档

## 1. 需求

- 一道错题 = **题目 + 解析**，一一对应、成对存储
- 题目和解析都支持 **文字 + 图片混排**（可直接 Ctrl/Cmd+V 粘贴截图）
- **翻页浏览**：一页一题，翻页即下一题
- **单机、离线、跨平台**（Windows / macOS / Linux），数据完全在本地

## 2. 技术选型

**推荐：Tauri 2 + TypeScript + React + Vite**

| 选择 | 理由 |
|------|------|
| Tauri 2 做外壳 | 跨平台桌面应用，安装包只有几 MB（Electron 要 100MB+），数据完全本地 |
| Web 前端做 UI | 「粘贴图片进编辑区」是浏览器原生能力（paste 事件），做图文混排最省力；翻页动画/遮罩交互也是 CSS 强项 |
| JSON 文件 + 图片文件夹做存储 | 个人题量（几百~几千）完全够用；整个数据目录用户可见、可直接拷贝备份、可放进网盘同步。不上 SQLite，题量大了再迁移 |

备选方案（不推荐但可行）：
- Electron：全 JS 生态更成熟，但体积大、内存占用高
- 纯网页 PWA：零安装，但数据绑在浏览器 IndexedDB，换浏览器/清缓存有丢失风险

## 3. 数据模型

内容采用「有序块」模型：题目/解析各是一个块数组，文字块和图片块交错，天然支持混排。

```ts
type Block =
  | { type: 'text'; text: string }          // 文字段落
  | { type: 'image'; hash: string; ext: string } // 图片，按内容哈希存盘

interface Mistake {
  id: string                 // uuid
  question: Block[]          // 题目内容
  analysis: Block[]          // 解析内容（与题目一一对应）
  tags: string[]             // 标签：科目、章节等
  createdAt: number
  updatedAt: number
  review?: {                 // 复习记录（M3 启用）
    streak: number           // 连续答对次数
    nextAt: number           // 下次应复习时间
  }
}
```

## 4. 存储布局

```
数据目录/（首次启动让用户选择，默认 ~/Documents/错题本/）
├── data.json      # 全部错题元数据与文字内容
└── assets/        # 图片文件，按内容哈希命名，如 3f2a….png
```

要点：
- **图片按内容哈希命名**：自动去重（同一张图贴到多题只存一份），data.json 里只存 hash，文件很小
- **原子写入**：保存时先写 `data.json.tmp` 再 rename 覆盖，防止写一半断电损坏
- **自动快照**：每次保存后把上一版挪进 `snapshots/`（保留最近 20 份），防误操作
- **备份 = 拷文件夹**：整个数据目录可以直接放进 iCloud/OneDrive，多台设备白送同步

## 5. 界面与交互

### 5.1 浏览模式（主界面，翻页）

```
┌────────────────────────────────────────┐
│ ←   数学 · 立体几何        第 12 / 87 题  → │
├────────────────────────────────────────┤
│                                        │
│    题目区域（文字 + 图片，可滚动）          │
│    图片点击放大                          │
│                                        │
├────────── 点击 / 下滑 展开解析 ─────────────┤
│ ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │
│ ░░░ 解析区域（默认遮罩盖住）░░░░░░░░░░░░ │
│ ░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░░ │
└────────────────────────────────────────┘
```

- **解析默认遮罩**：先自己想，再看答案——这是错题本的核心价值
- 翻页方式：`←`/`→` 键、空格、点击页边、触屏左右滑动，任选
- 翻页动画：滑动切换（translateX 过渡）即可，仿书页 3D 翻转可作为可选皮肤
- 顶部显示进度与当前标签筛选

### 5.2 录入 / 编辑模式

```
┌────────────────────────────────────────┐
│ 标签：[数学] [立体几何] ＋               │
├────────────────────────────────────────┤
│ 题目  [📎 插入图片]                     │
│ ┌────────────────────────────────────┐ │
│ │ 直接打字；Ctrl/Cmd+V 粘贴截图；      │ │
│ │ 拖拽图片文件进来；点 📎 从本地选择    │ │
│ └────────────────────────────────────┘ │
│ 解析                                    │
│ ┌────────────────────────────────────┐ │
│ │                                    │ │
│ └────────────────────────────────────┘ │
│                 [保存并录入下一题]        │
└────────────────────────────────────────┘
```

编辑器实现：**自己封装轻量块编辑器**，不引入 ProseMirror/Slate 等重型富文本库——
- 每个文本块是一个 contenteditable 段落，回车分段
- 图片不可编辑文字，只有「删除 / 上移 / 下移」三个操作

图片进入编辑区的**四条通道**（最终效果一致：在光标处插入图片块）：
1. **剪贴板粘贴**：Ctrl/Cmd+V 粘贴截图
2. **拖拽**：把图片文件拖进编辑区
3. **文件选择**：点 `📎 插入图片` 打开系统文件对话框，支持多选，按顺序插入
4. **路径输入**：在 📎 旁的路径框粘贴/输入本地路径（如 `~/Pictures/题1.png`），回车导入；也支持直接把文件从访达拖到路径框得到路径

### 5.3 批量导入

入口：录入页右上角「批量导入」按钮。面向「截图先攒在文件夹里、攒一波再录」的使用习惯。

流程：

1. **选择来源**：系统对话框选一个文件夹（或多选一堆图片文件）
2. **扫描**：遍历出所有图片（png/jpg/jpeg/webp/gif/bmp），按文件名**自然排序**（`题2` 排在 `题10` 前面），跳过非图片文件
3. **预览确认页**：

```
┌────────────────────────────────────────┐
│ 统一标签：[数学] [期末] ＋                │
├────────────────────────────────────────┤
│ ☑  题目图  缩略图  001.png              │
│ ☑  题目图  缩略图  002.png              │
│ ☑  解析图↩ 缩略图  003.png   （并入上一题）│
│ ☑  题目图  缩略图  004.png              │
│ …                                      │
├────────────────────────────────────────┤
│              [导入 4 题]    取消         │
└────────────────────────────────────────┘
```

   每行可选两种身份（默认「新错题·题目图」）：
   - **题目图**：新开一道错题，图片作为题目
   - **解析图 ↩**：不新开题，并入**上一行**那道题的解析——截图时题目、解析连着截的场景，勾一下就配好对

4. **执行导入**：逐张 读文件 → 算哈希 → 拷入 assets/ → 生成/合并错题，进度条实时显示；每完成一题就原子写一次 data.json，中途取消/断电，已导入的都还在
5. **完成**：提示「已导入 N 题」，可选择跳到浏览模式从第一题开始补解析，或留在录入页

要点：
- 已在库里的图（哈希相同）直接复用，不重复占用空间
- 导入后每题的解析为空（或为勾了「解析图」的图），在浏览模式里解析为空的题给出「去补解析」提示

### 5.4 管理列表（M2 再做）

缩略图网格或列表视图：搜索文字、按标签筛选、编辑、删除（进回收站，可恢复）、导出。

## 6. 关键实现点

1. **图片输入**：四条通道——剪贴板粘贴（`paste` 事件拦截 `image/*`）、拖拽（`drop` 事件）、系统文件对话框（tauri-plugin-dialog，支持多选）、路径框输入（读取该路径）。无论哪条通道，都走同一流程：读取字节 → 算内容哈希 → **拷贝**进 `assets/` → 光标处插入图片块。注意是拷贝不是引用原路径——原图被移动/删除不影响错题本；同一张图重复导入时哈希去重，只存一份
2. **翻页性能**：只渲染当前页 ±1，图片懒加载（IntersectionObserver）
3. **数据读写**（Tauri 侧）：tauri-plugin-fs 读写 JSON 与图片；保存做防抖（停止输入 500ms 后自动保存）
4. **快捷键**：`N` 新增、`E` 编辑当前题、`空格` 展开解析、`←/→` 翻页
5. **防丢数据**：原子写入 + 快照之外，删除需二次确认

## 7. 迭代路线

| 里程碑 | 内容 |
|--------|------|
| **M1 最小可用** | 录入（文字 + 四通道图片导入：粘贴/拖拽/文件选择/路径）、**批量导入**（文件夹扫描、题目图/解析图配对）、翻页浏览、解析遮罩、标签、翻页键盘/滑动 |
| **M2 管理** | 列表页、搜索、标签筛选、回收站、导出 zip 备份 |
| **M3 复习** | 自测模式（认识/不认识按钮）、简化版 SM-2 间隔重复算法、到期题优先排队、统计面板 |

## 8. 增补设计（v0.2）：文件夹管理 + 多标签筛选

### 8.1 数据模型（data.json 升级到 v2）

```ts
interface Folder {
  id: string
  name: string
  parentId: string | null   // null = 根层级，支持多层嵌套
  createdAt: number
}

interface Mistake {
  // …原有字段
  folderId: string | null   // null = 未分类
}
```

v1 旧数据打开时自动迁移：补 `folders: []`、`folderId: null`，下次保存即落盘为 v2。

### 8.2 交互

- **浏览页左侧常驻侧栏**：
  - 固定项「全部错题」「未分类」（含计数）
  - 文件夹树：多层缩进、可展开/收起；计数含所有子文件夹；悬停显示 ➕新建子文件夹 / ✎重命名 / 🗑删除
  - 删除文件夹：其中错题移入「未分类」，子文件夹上移一级（有确认提示）
  - 标签列表：显示每个标签的题数，**多选筛选**，多个标签取交集（同时包含所有所选标签）
- **顶栏面包屑**：显示当前文件夹完整路径（如「数学 / 立体几何」），激活的标签筛选以可点击的 chip 展示，点击移除
- **移动错题**：浏览页底部「移动」按钮 → 弹窗选目标文件夹；录入/编辑页也有文件夹下拉（缩进展示层级）
- **批量导入**：第 ③ 步可统一选择目标文件夹，与标签一起应用到本次全部导入
- 浏览某个文件夹时，列表包含其所有子文件夹中的错题（文件系统心智模型）

### 8.3 实现要点

- 文件夹只支持「新建/重命名/删除/新建子文件夹」，不做拖拽移动（章节结构基本一次建好，降低复杂度；后续需要再加）
- 纯函数层 `lib/folders.ts`：group/flat/路径名/后代集合/计数，供侧栏、下拉、筛选共用

### 8.4 批量导入增强（v0.3）：从文件夹批量导入

- **递归扫描**：来源文件夹连同所有子文件夹一起扫入（跳过 `.` 开头的隐藏目录）
- **按源结构落位**：默认「按源文件夹结构自动创建子文件夹」——在所选目标文件夹下，按来源的相对路径逐层 `find-or-create` 同名文件夹（已存在直接复用，不重复建）；关闭开关后，预览里每个源子文件夹分组可单独指定目标
- 预览按源子文件夹分组展示，组头显示落位去向；文件按「子文件夹路径 → 文件名」自然排序
- 「解析图 ↩」只允许并入**同一源子文件夹**里的上一道题，避免跨文件夹错配

### 8.5 编辑器多图增强（v0.3.1）

- 题目/解析均可插入任意多张图片，按触发顺序落在锚点之后；图片块 ↑↓ 调序、✕ 删除
- 修复并发导入丢块 bug：图片导入是异步写盘，此前连续两批导入（或导入期间继续打字）会用旧闭包覆盖新内容。现在块列表维护实时镜像（blocksRef），所有块操作基于镜像执行；多批导入进串行队列，按触发顺序依次落位
- 路径框支持一次粘贴多个路径（逗号/换行/中文逗号分隔）
- 粘贴「访达复制的图片文件 / file:// 地址 / 绝对图片路径」直接识别为导入（仅当所有片段都是图片路径时才拦截，普通文字粘贴不受影响）

### 8.6 标签录入增强（v0.3.2）

- **浏览页直接打标签**：当前题的标签行常驻，每个标签 chip 带 × 可移除；「＋ 标签」打开弹层——点选已有标签（再点取消）或输入新建，改动立即落盘（updateMistake）。翻页/切筛选自动收起弹层；弹层打开时屏蔽翻页快捷键，Esc 关闭
- **录入页/批量导入**：TagInput 在输入框下方常驻「＋ 已有标签」建议行（虚线 chip，点击即加，只显示尚未选中的），配合原有的输入新建（回车确认）与自动补全

### 8.7 组合筛选（v0.4）：文件夹 × 标签同时生效

- 文件夹与标签筛选本就叠加（文件夹范围 ∩ 标签条件）；v0.4 让它可感知：
  - **计数双向联动**：侧栏标签的计数限定在当前文件夹范围内（范围内为 0 的标签置灰但可点）；文件夹树/全部/未分类的计数随所选标签联动
  - **多标签匹配方式**：选中 ≥2 个标签时，标签区头部出现「同时满足（交集）/ 任一满足（并集）」切换
  - 空态文案显示完整组合条件（文件夹路径 + 标签及其匹配方式）

### 8.8 数据目录便携模式 + Windows 兼容（v0.4.1）

- Rust 侧新增 `exe_data_dir` 命令：返回可执行文件所在目录下的 `data` 文件夹
- 欢迎页平台差异化默认值：Windows 默认推荐「安装目录\data」（便携模式，配合 NSIS 用户级安装目录可写；Program Files 场景失败时提示改选），并保留文档目录与自定义选项；macOS 不提供该默认值（.app 包内写入会破坏签名且更新即丢），仍默认 `~/Documents/错题本`
- Windows 路径加固：`fileSrc()` 统一把路径分隔符归一为 `/` 再转 asset URL，图片显示与批量缩略图共用
- Windows 编译环境：Node ≥18 + Rust MSVC + VS Build Tools（C++ 桌面开发）+ WebView2；推荐 NSIS 安装包

## 9. 笔记模块

### 9.1 双一级导航（v0.4.3）

- 顶栏改为两个一级 tab：**错题本 / 笔记**（深色实心高亮）
- 「浏览 / 录入 / 批量导入」降为错题本内部的二级导航（分隔线右侧的浅色 pill），仅在错题本模式下显示；切到笔记再切回，二级位置保持
- 编辑页卸载兜底：切 tab 导致 EntryView 卸载时，若正在编辑已有错题且自动保存的 900ms 间隙里有未落盘内容，补存一次（新题仍只认显式保存）

### 9.2 数据目录合并导入（v0.5.0）

批量导入页新增「从另一份错题本数据目录合并」：

- 选择含 data.json 的目录 → 预览（总数/新导入数/跳过数/图片数）→ 一键合并
- **文件夹合并**：按「名称 + 父级」逐层 find-or-create，同名复用不重建（父层先处理，深度排序）
- **图片**：按内容哈希对比，当前 assets 缺的才从源目录拷贝；源里也缺的保留引用跳过拷贝
- **错题**：保留原 id 与时间戳（幂等：id 已存在的跳过，可重复执行），每个所属文件夹都映射到合并后的目标文件夹（v0.8 起多文件夹归属，见 §13）
- store 新增 addMistakes 批量追加；标签随错题自带（当时无独立标签表，v0.8 起预建标签也随库合并，见 §11）
- v0.5.1：合并入口自动定位数据目录——所选文件夹本身无 data.json 时向下扫描两层（整包拷贝的「错题本」文件夹、含 data/ 的安装目录均可直接选中），找到多个时提示用户选具体目录

### 9.3 笔记编辑与导出（v0.6.0）

**数据模型**（data.json 升 v3，旧版本自动迁移）：

```ts
interface Note {
  id: string
  title: string
  format: 'markdown' | 'word'   // markdown 存源文本；word 存富文本 HTML
  content: string               // 图片引用统一为 assets/<hash>.<ext> 相对路径
  createdAt: number
  updatedAt: number
}
```

- 图片与错题共用 assets（哈希去重）；content 里只存相对引用，显示时换成 asset 协议 URL，序列化时换回来——笔记数据随目录整体迁移/合并不受影响
- **两种编辑器**：
  - Markdown（marked 渲染，gfm + breaks）：编辑/分屏/预览三模式，useDeferredValue 保证大文档打字不卡；回车续列表前缀、Tab 缩进、B/I/码/❝ 快捷包裹
  - Word 富文本（contenteditable + execCommand，styleWithCSS=false 产出语义标签）：标题层级/加粗斜体下划线删除线/列表/引用/分隔线/清除格式；可粘贴网页与 Word 的富文本
  - 两者都支持粘贴截图、拖入文件、📎 选文件、粘贴访达复制的图片路径（与错题编辑器同套路）
- **自动保存**：停止输入 800ms 落盘 + Ctrl/⌘+S 立即保存 + 卸载兜底；新建空笔记（无标题无内容无图）不落盘直接丢弃
- **导出**（jsPDF + html2canvas 按需动态加载，不占启动包体）：
  - PDF：离屏 A4 宽（794px@96dpi）容器渲染 → html2canvas 2x 位图 → 按 A4 高切片分页；文字为位图
  - Word：MHTML 格式 .doc——HTML 正文与图片全部 base64 内嵌单个 MIME 文档，Word/WPS 直接打开，不依赖外部图片文件
- 合并导入同步支持笔记：按 id 幂等去重，笔记引用的图片一并拷贝

## 10. 远程同步（v0.7）：推送上传

顶栏「☁ 同步」→ 弹窗填服务器地址（`ip:端口`，缺 `http://` 自动补；可勾选「记住此地址」存 localStorage 键 `errorbook.sync.server`，随时改填新的）→ 增量推送整库到自建服务端。协议文档（服务端实现规范）见 docs/sync-protocol.md。

- **不打 zip，走哈希差量**：图片本来就按内容哈希命名（assets/<sha1>.<ext>），客户端先 `GET /sync/manifest` 问服务端已有哪些图片，只 `PUT` 缺的，最后 `PUT /sync/data` 推整份 data.json（几 KB）——天然增量、幂等、可续传，服务端零压缩/解压逻辑
- **服务端落盘即合法数据目录**（data.json + assets/），与本地布局一致，可直接被「合并导入」消费
- **网络层**：tauri-plugin-http（前端 fetch 直连，绕过 webview CORS）；capabilities 里 http:default 放开 http/https 任意地址（局域网自建服务场景）
- **服务端实现**：backend/sync_server.py（Python 标准库零依赖，含状态页 GET /、令牌、原子写），部署说明 backend/README.md；协议规范 docs/sync-protocol.md
- **实现**：src/lib/sync.ts（协议客户端：地址规范化、清单差量、逐张上传带进度与中止、错误分类——连不上 / 非同步服务（404 或响应非 {assets}）/ 令牌拒绝（401/403）/ 上传失败）；src/components/SyncDialog.tsx（弹窗 UI，复用 modal/import-progress 样式）
- **中止语义**：图片逐张串行上传，中止只在图片间隙生效；已上传的在下次同步里经 manifest 自动跳过
- **失败兜底**：本地引用了但文件缺失的图片跳过不阻断（与合并导入口径一致）；data 推送在图片全部完成后才执行，服务端不会拿到引用了却缺图的库（除非本地本来就缺）

### 10.1 从远程拉取合并（v0.8，协议 v2）

弹窗改为「⬆ 推送到远程 / ⬇ 从远程拉取」双模式，iOS 端同步页同样双模式。拉取语义是**合并而非覆盖**——与「合并导入」同一套核心逻辑（桌面端抽成 src/lib/merge.ts，iOS 端 MergeImport.swift 的 runMergeCore，目录合并与远程拉取两个入口共用）：

- **两步交互**：先「检查并预览」——`GET /sync/data` 取远端整库（几 KB），客户端本地算差量（将新增几道错题/笔记/文件夹/标签、下载几张图片），确认后才执行；差量为零时提示「已是最新」
- **执行**：逐张 `GET /sync/asset/<name>` 只下载本地缺的图片（哈希命名幂等，中止后重拉自动续上；服务端也缺的保留引用跳过），然后错题/笔记按 id 去重入库、文件夹按「名称+父级」合并、预建标签并入——**只增不删，不覆盖本地任何数据**
- **服务端新增 2 端点**（GET /sync/data、GET /sync/asset/<name>），纯追加不破坏 v1；服务端对拉取只读，合并完全在客户端完成
- **多端用法**：每端各自「推送」再「拉取」即可互相同步（服务器是交汇点，不做自动冲突解决；同 id 错题以先入库者为准，编辑冲突靠 updatedAt 手动判断）

## 11. 预建标签（v0.8）

标签此前完全由错题自带（见 §9.2），想先建好标签体系只能借道错题。现给 Database 增加可选字段 `tags`（v3 内的增量字段，旧数据缺失按空处理；桌面端与 iOS 端模型同步支持，加载-保存不丢字段）：

```ts
interface Database {
  version: 3
  mistakes: Mistake[]
  folders: Folder[]
  notes: Note[]
  tags: string[]   // 预建独立标签：不挂在任何错题上也存在
}
```

- **入口**：桌面端侧栏标签区「＋ 新建」（NameModal，placeholder 参数化）；iOS 端浏览页标签筛选菜单「新建标签…」（alert 输入）。空名或与已有标签（错题自带 / 已预建）重名时静默跳过
- **展示**：预建标签计数 0，侧栏/筛选列表置灰可点（与「当前文件夹范围内无此标签」同一形态）；录入页 TagInput 建议行、批量导入统一标签、浏览页打标签弹层自动带上
- **合并导入**：源库 `tags` 整体并入（store.addTags 去重——跳过当前已有与随新错题带进来的），预览与结果文案计入
- **远程同步**：走整份 data.json，协议无需改动

## 12. 浏览排列（v0.8）：时间 / 随机

翻页浏览的排列方式此前是插入顺序（数组序），现改为显式可切，桌面端与 iOS 端一致：

- **两种排列**：按录入时间（createdAt 升序，旧 → 新，默认）或随机（复习防背序）
- **稳定洗牌**：随机用 mulberry32 种子洗牌（seed 存组件 state），同 seed 同列表 → 同顺序——列表 memo/重算不会中途跳序，翻页稳定；换 seed 才换序（进入随机模式或点「重新洗牌」）
- **入口**：桌面端浏览页底栏左侧分段按钮「⏱ 时间 / 🔀 随机」（随机激活时再点 = 换一批）；iOS 端筛选栏右侧时钟/洗牌图标菜单（含「重新洗牌」）。排列方式持久化：桌面 localStorage 键 `errorbook.browse.order`，iOS @AppStorage `browseOrder`（seed 不持久化，每次启动重新洗）
- 切换排列/重新洗牌后回到第一题；文件夹与标签筛选不受影响，排列只作用于筛选结果

## 13. 多文件夹归属（v0.8）

一道题从「属于一个文件夹」改为**可同时属于多个文件夹**（标签本就多值，不动）：

- **数据模型**：`Mistake.folderId: string | null` → `folderIds: string[]`（空数组 = 未分类）。读写兼容：加载/解码时旧的单值 folderId 自动迁移为 `[folderId]`；保存只写新格式。v3 内的格式演进，旧版客户端读到新数据会丢失文件夹归属（两端需一起更新）
- **入口**：录入/编辑页文件夹改多选弹层（桌面 FolderSelect 弹层复选树 / iOS FolderMultiPicker 勾选 sheet）；浏览页「移动到文件夹」改为「调整所属文件夹」（勾选式，全部取消 = 未分类）。批量导入的目标文件夹仍为单选（每组一个目标，导入后可再补选）
- **筛选与计数**：浏览按文件夹筛选命中任一所属（含子文件夹口径不变）；侧栏计数在多个文件夹重复计入（各文件夹之和可大于总数，与标签计数同口径）；「未分类」= 没有任何有效所属
- **删除文件夹**：只从错题的所属列表里移除该文件夹，其余所属保留，全部移空的落到未分类
- **合并导入 / 远程拉取**：源库每个所属文件夹都经「名称+父级」映射落到目标（映射不到的丢弃）；文件夹树合并逻辑不变

### 13.1 拖放归类（v0.8，桌面端）

侧栏文件夹树与浏览页卡片之间支持 HTML5 拖放（自定义 MIME 负载 `application/x-errorbook-mistake/folder`，与文本/图片拖拽互不干扰）：

- **错题 → 文件夹**：浏览页按住当前题卡片拖到侧栏文件夹上松手——默认**移动**（所属替换为该文件夹）；按住 ⌥/Ctrl 落下 = **追加所属**（多文件夹）。拖到「未分类」= 清空所属
- **文件夹 → 文件夹**：侧栏里拖文件夹 A 到 B 上 = A 重挂为 B 的子级（store.moveFolder，防环：不能落到自己或自己的后代上，父级未变跳过）；拖到「全部错题」= 移到顶层
- 可落目标在 dragover 时虚线高亮（.drop-target）；卡片内图片设 draggable=false 防原生图片拖拽劫持；iOS 端无侧栏树，归类仍走「调整所属文件夹」弹层

## 14. 选择题选项与错误率（v0.8）

带选项的题可以在浏览时作答，自动统计错误率；无选项的题完全不参与：

- **数据模型**：Mistake 增加 `options: string[]`（选项文案，空 = 非选择题）、`answer: number | null`（正确选项下标）、`attempts / wrong: number`（作答统计，错误率 = wrong/attempts，未作答显示 0%）。旧数据缺字段按空/0 迁移；统计随库同步/合并原样保留
- **录入页**：「选项（选填）」编辑器——动态增删（最多 8 项）、每项前 ○ 单选标记正确答案；填了选项必须标记正确答案才允许保存（提示或清空）；连续录入下一题时选项清空、统计归零起步
- **浏览页**：题目下方显示选项按钮（A/B/C…），点击即作答——答对绿、答错红并标出正确答案，答完自动翻开解析；每次作答 attempts+1（答错再 wrong+1）立即落盘。选项区头部显示「错误率 N%（wrong/attempts）」
- **错误率排序**：浏览排列第三种模式（⏱ 时间 / 📊 错误率 / 🔀 随机），只列出**有选项且标记了正确答案**的题，按错误率降序 → 作答次数降序 → 录入时间升序；无选项的题不参与该排序（切回时间/随机可见）
- 批量导入的截图题默认无选项，可在编辑页补填

## 15. 一键复制整道错题（v0.8）

浏览页一键复制，两个范围可选：**只复制题目**（题目文字 + 图片）或**复制全部内容**（题目、选项（含正确答案标记）、解析（文字 + 图片）、标签、所属文件夹）。

- **双格式剪贴板**：同时写 text/html + text/plain 两个分量——粘贴到 Word / 笔记 / 邮件等支持富文本的地方图随文走（图片以 base64 data URL 内嵌，不依赖本地文件）；粘贴到纯文本场景自动落到纯文本版（图片以 [图片] 占位）
- **桌面端**：浏览页底栏「复制 ▾」向上弹出菜单二选一（lib/copy.ts 的 CopyScope），复制后提示范围与富文本/纯文本兜底；**iOS 端**：卡片菜单「只复制题目（含图片）/ 复制全部内容」（CopyExport.swift，UIPasteboard 双分量，questionOnly 参数）
- 图片文件缺失时该位置以「[图片缺失]/[图片]」占位，不阻断复制

## 16. 录入选项默认可见 + 筛选条件栏（v0.8）

- **录入页选项默认摆出 A–D 四个空框**（此前初始为空只有一个「＋ 添加选项」按钮，不显眼）：点 ○ 标记正确答案；全留空保存即非选择题（不参与错误率）；保存下一题恢复空白 A–D；编辑已有题带出已存选项
- **浏览页筛选条件一目了然**：激活的条件以 chip 列在顶栏——文件夹条件（📁 路径 ✕，点击单独取消）、各标签（✕ 单独移除），末尾「⟲ 重置」一键清空全部条件（文件夹回全部、标签清空）；iOS 端筛选栏同样（chip 行 + 红色重置按钮）

## 17. 做题 tab（v0.8）：外部刷题答题卡

第三个一级 tab（错题本 / 笔记 / ✍️ 做题；iOS 底部 tab 同步新增）。面向纸质/外部刷题场景，App 当答题卡用：

- **流程**：设置（文件夹名 + 题数 1–100，可从已有文件夹名中选）→ 逐题选 A/B/C/D 作答（每题可 ⭐ 标记「值得导入」）→ 做完输入正确答案（可逐题手输，也可**批量导入**：粘贴每行「题号<Tab>答案」的文本一键填入，分隔符兼容 Tab/空格/逗号，越界/无效行自动跳过并计数；没对的按「未比对」处理，不算错）→ 比对结果
- **结果页**：答对/答错/未答/未比对统计与正确率；导入采用**勾选制**——候选（答错 ∪ 标记 ⭐）默认预勾「是否导入」，每题可勾/取消（错题不必非得导入），「导入勾选的 N 题」只导勾选的；全部题号以彩色网格总览（绿=对、红=错、角标=已导入/⭐）
- **导入语义**：在会话文件夹按名称 find-or-create（留空 = 未分类），生成占位错题——题干「第 N 题（做题导入，待补充题目内容）」，解析里写批改记录（我选 X，正确答案 Y + 时间），之后在错题本编辑页补真实题目内容与图；文件夹/标签/同步等一切按普通错题处理
- **会话保持**：桌面端会话提升到 App 层（切 tab 不丢）；iOS 端会话存于 tab（中途退出全屏页可「继续上次做题」）；会话不持久化到磁盘，重启 App 重新开始

### 17.1 待导入清单：手机做题 → 电脑导入（v0.8）

配合「手机上做题、电脑上整理错题」的工作流，做题结果可以不直接导入，而是变成跨设备流转的清单：

- **数据模型**：Database 增加可选字段 `pendingImports: PendingImport[]`（id / folderName / createdAt / total / entries{no, mine, key, flagged}），随 data.json 走现有同步与合并通道（按 id 去重），协议不变
- **手机端（iOS）**：做题结果页新增「存为待导入清单（推送后在电脑导入）」——把勾选的题（题号、我的答案、正确答案、⭐ 标记）连同文件夹名存进库；之后正常「☁ 同步 → 推送」即上服务器
- **电脑端（桌面）**：「☁ 同步 → 从远程拉取」合并清单后，做题 tab 设置页下方出现「来自手机做题的待导入清单」——每份显示文件夹、题数、待导入题号，可「导入 N 题」（在对应文件夹生成占位错题，批改记录写进解析）或「丢弃」；导入完成清单自动移除
- 手机上仍可保留原来的直接导入路径；清单只是多了一种「拿回电脑慢慢补内容」的选择

## 18. 浏览页题号总览（v0.8.2）

浏览时把当前筛选（文件夹 + 标签）下的全部题一键展开成题号网格，点题号直接跳到那道题——一个标签里有哪几题、一个文件夹里攒了多少错题，一眼看清，不用一页页翻：

- **入口**：桌面端顶栏「▦ 第 N / M 题」进度字样可点；iOS 端底栏页码（带网格图标）可点，打开题号总览弹层
- **网格**：题号 = 当前排列（时间 / 错误率 / 随机）下的序号；配色沿用做题结果页的直觉——红 = 错过（wrong > 0）、绿 = 作答全对、灰 = 未作答 / 非选择题；当前题蓝框描边，打开时自动滚到当前题
- **跳转**：点题号关闭弹层并翻到那道题（作答与翻开状态照常重置）；筛选条件或排列方式变化后总览随之更新

## 19. Android 端（v0.8.2）

`android/` 目录是 iOS 端的原生镜像（Kotlin + Jetpack Compose + Material 3，单 Activity 四 tab），数据层与交互口径逐条对齐 iOS 端实现（Models / BookStore / ImageStore / Folders / Markdown / MergeImport / SyncEngine / CopyExport / NoteExporter / Components / 各 View 一一对应），功能与已知边界见 docs/android.md。平台能力映射：

| iOS | Android |
| --- | --- |
| SwiftUI TabView 四 tab | Scaffold + NavigationBar 四 tab（movableContent 保活） |
| PhotosPicker 相册选图 | Photo Picker（PickVisualMedia / PickMultipleVisualMedia） |
|「文件」App 选文件夹（安全作用域）| SAF（OpenDocumentTree + DocumentFile 递归扫描，持久化 Uri 权限） |
| UIPasteboard 双分量（plainText + HTML）| ClipData.newHtmlText（纯文本 + HTML 双分量） |
| UIMarkupTextPrintFormatter 原生分页 PDF | WebView + PrintManager 系统打印框架（A4、文字可选中，「保存为 PDF」） |
| 沙盒 Documents/错题本（文件 App 可见）| 外部私有目录 Android/data/…/files/错题本（免权限、USB/MTP 可拷，卸载即删） |
| NSAllowsArbitraryLoads 放行局域网 http | usesCleartextTraffic |
| @AppStorage | SharedPreferences（键名沿用桌面/iOS：browseOrder、practiceFolderName、entryMemory*、errorbook.sync.server） |

实现约定：数据读写全部走同一套宽容序列化（字段缺失/null/类型不符回退默认值，旧 folderId 单值自动迁移）；落盘为原子写 + snapshots 限流 20 份，与桌面/iOS 完全同构；纯逻辑层（序列化、Markdown 渲染、做题答案解析、文件夹树、稳定洗牌、合并差量）带 JVM 单元测试（`./gradlew testDebugUnitTest`）。
